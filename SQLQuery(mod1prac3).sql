create table jobs(
	job_id int primary key ,
	job_title varchar(255) default '',
	min_salary int default 8000,
	max_salary int 
	);

insert into jobs(job_id) values(3)

select * from jobs


drop table jobs

